# Frontend

**Scaffold placeholder** riservato alla futura interfaccia utente di PitWall.AI
(migrazione post-esame, es. FastAPI + Next.js). **Directory intenzionale, non dead code.**

Allo stato attuale l'app gira su **Streamlit** (`app.py` + package `ui/`); tutta la
logica server e i moduli di dominio si trovano in `backend/`. Questa cartella verrà
popolata quando si separerà il frontend dal backend.
